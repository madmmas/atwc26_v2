# Copy to backend.hcl (gitignored) for remote Terraform state.
# Used by CI deploy workflow and optional local apply.

bucket  = "your-tf-state-bucket"
key     = "atwc26-v2/dev/terraform.tfstate"
region  = "us-east-1"
encrypt = true
